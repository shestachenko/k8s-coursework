apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secret.yaml
  - oci-repository.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: n8n-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
